pipeline {
    agent any
    stages {
        stage('Build gtoolkit') {
            steps {
                build '/gtoolkit'
                echo "gtoolkit build done."
            }
        }
    }
}